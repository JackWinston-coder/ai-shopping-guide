# 开发日志 - 2026-05-29

## 今日目标

- 按执行规则推进 Milestone 5 的下一个小步：补齐 Agent Prompt 模板文件
- 保持现有 Agent 业务流程不扩范围，只做模板化与窄验证
- 优化 RAG 中“跑步/咖啡/油皮”等意图词的查询规则，减少硬过滤误伤
- 完成 Milestone 6：前端对接后端 Chat API、购物车/订单 REST API、图片找货、语音输入和 TTS 播报

## 已完成

- 新增 `server/llm/prompts.py`，提供带缓存的 Prompt 模板读取方法
- 新增 Router/Guide/Cart/Order 四个 Agent 的 Markdown Prompt 模板
- 将 RouterAgent、GuideAgent、CartAgent、OrderAgent 改为从模板文件读取 system prompt
- 顺手修复 `BaseAgent._build_tool_messages` 中异步工具执行未正确等待的潜在问题
- 通过 `python -m compileall server` 编译验证
- 新增 `server/rag/query_profile.py`，把查询词拆成 category/sub-category/硬词/加权词画像
- 调整 RAG 检索与重排，优先用显式画像做加权，只有明确商品词才做硬约束
- 让本地 embedding 的 domain boost 复用同一套查询规则
- 修复 RAG 查询中 category hint 未下传到向量检索的遗漏
- Chat API 支持 `image_url`：先调用智谱图片理解，再把图片特征并入导购消息进入 RAG 流程
- Agent 编排器会持久化 `product_cards` 和 `tool_result`，支持下一轮“第二个/刚才那个”等指代上下文
- 新增 `OutputValidator`，对助手回复中的商品引用做基础后验校验
- 新增 `client/lib/api.ts`，统一前端登录、JWT、Chat SSE、购物车、订单 API 调用和数据映射
- 登录页改为调用真实 `/api/auth/login`，保存后端 JWT
- 聊天页接入真实 `/api/chat/stream`，处理 `session/text_delta/product_cards/error` SSE 事件
- 聊天页商品卡片和详情页加购按钮接入真实购物车 REST API
- 购物车抽屉接入真实查看、数量更新、移除 API
- 订单确认页接入真实订单预览和创建订单 API，提交后展示后端订单号
- 图片上传支持选择本地图片并以 data URL 传给 Chat API 的 `image_url`
- 语音输入接入浏览器 Web Speech API，助手回复播报接入浏览器 SpeechSynthesis
- README 当前阶段更新为 Milestone 6

## 遇到的问题

- 首次编译发现 `_build_tool_messages` 中出现 `await outside async function`，已将该辅助方法改为 async
- PowerShell 里直接喂中文 heredoc 测试会变成乱码，样例验证改成 Unicode 字面量方式
- 前端 build 通过，但 Next.js 提示工作区存在多个 lockfile，当前只是警告，不影响构建
- 订单页在购物车为空时后端预览会返回错误，前端保留 toast 提示和空状态展示

## 风险与决策

- 本次仅完成 M5 Day 16 中的 Prompt 模板创建与小范围基类修复，不进入前端联调、多模态或部署阶段
- 现有 Agent 仍保留原业务逻辑，模板外置后便于后续按 Router/Guide/Cart/Order 单独调优
- RAG 规则从“关键词命中即硬过滤”改为“查询画像 + 软硬分离”，更适合扩展相近意图词
- M5 收口优先保证 Chat API 后端闭环；前端联调和语音输入属于 M6，不在本次扩范围
- M6 前端优先完成真实闭环，不改整体 UI 架构；语音输入/TTS 使用浏览器原生能力，不新增后端 ASR/TTS 服务
- 图片找货复用后端 `image_url` 能力，前端以 data URL 传入，后端视觉理解后继续走 RAG

## 明日待办

- 进入 Milestone 7：Docker 部署配置 + 本地一键启动
- 补充演示脚本和端到端测试用例
- 整理答辩材料和最终 Demo 路线
